# Runtime dependencies for the deployed FastAPI service (what Render installs).
# Kept slim on purpose: no test/dev tooling, no Streamlit — those live in
# requirements-dev.txt. A lean runtime = a fast, reliable free-tier build.
PyYAML>=6.0
anthropic>=0.39
prometheus-client>=0.20
fastapi>=0.110
uvicorn[standard]>=0.29
